template(name='nav')
  nav(class="navbar navbar-default navbar-thin-height navbar-fixed-top" role="navigation")
    div(class="container-fluid")
      div(class="navbar-header")
        button(type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#mobile-nav-collapse")
          span(class="icon-bar")
          span(class="icon-bar")
          span(class="icon-bar")
        a(class="navbar-brand" href="{{ pathFor 'lobbyLayout' }}")
          img(alt='brand' src='/images/icon.jpg')

      //- !-- Collect the nav links, forms, and other content for toggling --
      div(class='collapse navbar-collapse' id='mobile-nav-collapse')
        ul(class="nav navbar-nav")
          li
            a(href="{{ pathFor 'lobbyLayout' }}" class="{{ isActiveRoute regex='lobbyLayout' }}") LOBBY
          li
            a(href="{{ pathFor 'chatterLayout' }}" class="{{ isActiveRoute regex='chatterLayout' }}") TALK
          li
            a(href="{{ pathFor 'nflPlayersListLayout' }}" class="{{ isActiveRoute regex='nflPlayersListLayout' }}") PLAYERS
          li
            a(href="{{ pathFor 'upcomingContestListLayout' }}" class="{{ isActiveRoute regex='upcomingContestListLayout' }}") UPCOMING
          li
            a(href="{{ pathFor 'liveContestListLayout' }}" class="{{ isActiveRoute regex='liveContestListLayout' }}") LIVE
          li
            a(href="{{ pathFor 'historyContestListLayout' }}" class="{{ isActiveRoute regex='historyContestListLayout' }}") HISTORY
          +_navAdmin
          li
            a(href="{{ pathFor 'historyContestListLayout' }}" class="{{ isActiveRoute regex='historyContestListLayout' }}") $0.00
        ul(class="nav navbar-nav navbar-right")
          div(class="login-block")
            if user
              +avatarImage
          div(class="login-block")
            +login
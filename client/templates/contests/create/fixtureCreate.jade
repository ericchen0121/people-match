template(name='fixtureCreate')
  div(class='col-xs-24')
    div(class='row')
      h1(id='contest-title' class='col-xs-16') Fixtures Creation Tool
    div(class='row col-xs-8')
      h3 Events
      +each allEvents
        div(class='event-options') {{momentify startsAt}} #{home} #{away}
      div(class='btn btn-lg btn-default create-fixture') Create Fixture
    div(class='row col-xs-8')
      h3 Fixtures
      +each fixtures
        div
          span
            h4 {{ momentify startsAt }}: {{ fixturesEventCount }} Events
        +each fixturesEvents
          div {{momentify startsAt}} #{away} @ #{home}

